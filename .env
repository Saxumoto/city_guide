# --- CRITICAL DJANGO SETTINGS ---
# FIX: The space after the equals sign has been removed, although Git handles it, 
# it's best practice not to include it in .env files.
SECRET_KEY=rJsU1Ex08rGtT8Dn70zUVBga77DX657RskNBJwjsR4jXvp4hy1

# Set to False for production security
DEBUG=True 

# Allowed hosts in production (must include your live Render URL, e.g., city-guide-app.onrender.com)
ALLOWED_HOSTS=127.0.0.1,localhost,PASTE_YOUR_FUTURE_RENDER_DOMAIN_HERE

# --- DATABASE CONFIGURATION (FIXED FOR LOCAL DEVELOPMENT) ---
# FIX: Changed the engine back to sqlite3 for local development
DB_ENGINE=django.db.backends.sqlite3 
DB_NAME=db.sqlite3

# PostgreSQL specific settings (must be empty or ignored for local SQLite to work)
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=